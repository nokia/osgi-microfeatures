#
# Indicates for each diameter application the group to route to

# Syntax:
# route -app <diameter-application> -group <group-name>
# It is possible to indicate many applications in the same line.
# Examples:
# route -app 123 -group mygroup1
# route -app 234 -app 456 -app 567 -group mygroup2
# a diameter application can be prefixed with 0x to indicate an hexa value
# route -app 0xA1F -group mygroup
#
# optionally, a default route can be set:
# route -def -group <group-name>
# Example:
# route -def group mydefgroup
#

