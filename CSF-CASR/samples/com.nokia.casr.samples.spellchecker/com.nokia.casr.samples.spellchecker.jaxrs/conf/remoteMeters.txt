#
#
# Indicate the list (one per line) of monitorables/meters to fetch locally.
#
# Examples:
#get -m as.ioh.engine.sip -mt cpu.system -mt cpu.process -p 1000
#get -ms sip -mts * -p 1000
# -m : monitorable
# -ms : monitorables pattern
# -mt : meter
# -mts : meters pattern
# -p : period (in ms) : set 0 for notif on change
#


